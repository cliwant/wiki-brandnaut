# 브랜드넛 입찰 지식 베이스

이 저장소는 brandnut의 공공 입찰 지식 베이스다.
제안서를 만들 때 여기의 지식이 참조되고, 제안서 작업에서 새로 확인된 사실이 다시 여기로 쌓인다.
쌓일수록 다음 제안이 빨라지고 근거가 강해진다.

## 3계층 구조

1. `sources/` — 원본 자료. 불변이며, 갱신은 날짜 찍힌 새 파일을 추가하는 방식으로만 한다.
2. 페이지 — `profile.md`(회사) · `sectors/`(시장) · `bids/`(공고). 원본에서 종합된 지식.
3. 이 문서 — 유지 규칙.

## 불변 규칙

- 페이지의 모든 사실에는 출처(sources 경로)·수집일·confidence(high/medium/low)를 병기한다.
- sources는 수정하지 않는다. 새 정보는 새 파일로 추가한다.
- 본문은 한국어로 쓰고, 디렉토리·파일명은 소문자 영문 slug를 쓴다. 한글 표시명은 frontmatter의 title에 둔다.
- 집계·검사에 쓰이는 값은 반드시 frontmatter 고정 필드에 둔다. 본문 서술에 묻지 않는다.
- 모든 변경은 `log.md`에 한 줄씩 append하고 `index.md`를 갱신한다. log는 절대 수정하지 않는다.

## 지식의 자리

- 회사가 존속하는 한 유효한 사실(자격·실적·역량·제약·의도) → `profile.md`
- 특정 시장에서 계속 유효한 사실(경쟁 구도·발주 관행) → `sectors/<slug>/`
- 특정 공고에서만 유효한 사실(요건·판정·전략) → `bids/<공고ID>.md`

## 공고 카드의 수명

공고 카드의 status는 active → submitted → archived 순서로만 흐른다.
결과(result)가 확정되면 오래갈 통찰을 섹터·프로필로 승격한 뒤 archived로 전환한다.

## confidence 기준

- high: 문서 원본·공식 DB 근거
- medium: 검증되지 않은 진술(인터뷰 등)
- low: 증빙 없는 실적·수치 주장 — 증빙이 sources에 들어오면 상향한다
- 객관 데이터와 진술이 충돌하면 객관이 이긴다. 진술끼리 충돌하면 최신을 쓰되 이전 값을 log에 남긴다.

## 스키마

이 저장소의 frontmatter 스키마 버전은 `wiki.yml`에 기록돼 있다.
스키마 정의와 유지보수 도구는 contrl 플러그인이 제공하며, 이 저장소에는 데이터만 둔다.
